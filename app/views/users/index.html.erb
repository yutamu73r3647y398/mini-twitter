<% provide(:title, "All users") %>
<h1>All users</h1>

<!--ページネーションメソッド,Gemfile参照-->
<%= will_paginate %>

<ul class = "users">
  <% @users.each do |user| %>
  <li>
    <%= render @users %>
  </li>
  <% end %>
</ul>

<%= will_paginate %>
